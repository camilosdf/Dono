# Deploy do Sistema Dono em Produção

## Pré-requisitos
- Docker e Docker Compose instalados.
- Arquivo `.env` preenchido com credenciais (veja `.env.example`).
- (Opcional) Certificado SSL para HTTPS.

## Passos

1. **Clone o repositório** no servidor:
   ```bash
   git clone https://github.com/seu-usuario/dono.git
   cd dono